import { Box, Typography } from '@mui/material';
import React from 'react';

const Header = () => {
	return (
		<Box sx={{ bgcolor: '#3A80BA', minWidth: '100vw', minHeight: '10%' }}>
			<Typography>AppCo</Typography>
		</Box>
	);
};

export default Header;
